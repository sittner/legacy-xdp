PACKAGE_NAME="genet-xdp"
PACKAGE_VERSION="6.18.35+xdp1"

BUILT_MODULE_NAME[0]="genet_xdp"
BUILT_MODULE_LOCATION[0]="genet"
DEST_MODULE_LOCATION[0]="/updates/dkms"

AUTOINSTALL="yes"

MAKE[0]="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build/genet modules"
CLEAN="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build/genet clean"
