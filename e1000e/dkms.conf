PACKAGE_NAME="e1000e-xdp"
PACKAGE_VERSION="6.18.35+xdp1"

BUILT_MODULE_NAME[0]="e1000e_xdp"
BUILT_MODULE_LOCATION[0]="e1000e"
DEST_MODULE_LOCATION[0]="/updates/dkms"

AUTOINSTALL="yes"

MAKE[0]="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build/e1000e modules"
CLEAN="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build/e1000e clean"
