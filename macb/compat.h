/* SPDX-License-Identifier: GPL-2.0-only */
/*
 * macb-specific kernel version compatibility layer for out-of-tree builds.
 * Generic shims live in ../compat/compat.h; this file adds only
 * definitions that are unique to the macb driver.
 */
#ifndef _MACB_COMPAT_H_
#define _MACB_COMPAT_H_

#include "../compat/compat.h"

/* Add macb-specific compat shims below as needed. */

#endif /* _MACB_COMPAT_H_ */
